# MuDiKo KI Assistant - Environment Variables

# ========================================
# SECURITY (REQUIRED!)
# ========================================

# Flask application secret key - MUST be set in production!
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=REPLACE_WITH_A_STRONG_SECRET_KEY

# ========================================
# CORS Configuration
# ========================================

# Allowed Frontend origins (comma-separated)
# Development: http://localhost:5173,http://localhost:3000
# Production: https://music.ifib.eu
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ========================================
# SESSION MANAGEMENT
# ========================================

# Session timeout - sessions are deleted after this many seconds of inactivity
SESSION_TTL_SECONDS=3600          # 1 hour (3600 seconds)

# Garbage Collection interval - how often to clean up expired sessions
GC_INTERVAL_SECONDS=900           # 15 minutes (900 seconds)

# Maximum upload file size (in bytes)
MAX_CONTENT_LENGTH=104857600      # 100 MB

# ========================================
# OPTIONAL: OpenAI Integration
# ========================================

# OpenAI API Key (only needed if using AI feedback features)
# OPENAI_API_KEY=sk-your_api_key_here